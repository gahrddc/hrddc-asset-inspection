# บันทึกตรวจสอบระบบ 13 สิงหาคม 2569

## อาการที่ยืนยันได้

1. หน้า GitHub Pages โหลดโครงหน้าได้เร็วประมาณ 0.3 วินาที แต่รอข้อมูลรอบตรวจรวมประมาณ 3 วินาที
2. Google Apps Script API มีเวลาตอบผันผวน การทดสอบ `config` และ `find` อยู่ประมาณ 1.6–5.7 วินาที
3. การทดสอบ `config` 6 ครั้งพบ HTTP 200 จำนวน 4 ครั้ง และ HTTP 500 จำนวน 2 ครั้ง
4. การทดสอบต่อเนื่องบางคำขอไม่ตอบภายใน 12 วินาที จึงเป็นปัญหาฝั่ง Apps Script/redirect ร่วมด้วย ไม่ใช่เฉพาะหน้าเว็บหรืออุปกรณ์
5. หน้าแอดมินเรียก `getAdminNotifications` ทุก 30 วินาที แม้ไม่ได้เปิดการแจ้งเตือน แต่ละครั้งใน Execution log ใช้ประมาณ 1.3–8.7 วินาที ทำให้เกิดคิวและแย่งทรัพยากรกับหน้าใช้งาน
6. หน้าเว็บใช้ไลบรารีจาก `unpkg.com` แบบไม่ตรึงเวอร์ชัน (`latest`) จึงเสี่ยงต่อการเปลี่ยนพฤติกรรมเองและการโหลดต่างกันระหว่างเครือข่าย
7. การบันทึกเดิมมีโอกาสเขียนชีทสำเร็จแต่คำตอบกลับหาย ทำให้หน้าเว็บแสดงว่าล้มเหลว และเมื่อผู้ใช้ลองซ้ำอาจเกิดความสับสน

## การแก้ไขที่ทำในสำเนาท้องถิ่น

- ตรึง `html5-qrcode` เป็น 2.3.8 และ `lucide` เป็น 1.31.0
- ใช้ JSONP เป็นเส้นทางหลักเพื่อรองรับ Safari/Chrome มือถือ และตัด `fetch` ที่มีปัญหากับ redirect ของ Apps Script
- แสดงข้อมูลรอบจาก cache ทันที แล้วรีเฟรชข้อมูลจริงเบื้องหลัง
- จำกัด cache รอบตรวจไว้ 30 นาที เพื่อไม่แสดงรอบเก่านานเกินไป
- เพิ่ม `requestId` ต่อหนึ่งการบันทึก ป้องกันการบันทึกซ้ำเมื่อกดลองใหม่
- หากคำตอบบันทึกขาดหาย หน้าเว็บจะค้นหารายการนั้นเพื่อยืนยันก่อนแจ้งว่าล้มเหลว
- ฝั่งหลังบ้าน cache การแจ้งเตือน 45 วินาที, ไม่ยิงคำขอซ้อน, ลด polling จาก 30 เป็น 90 วินาที และหยุด polling เมื่อแท็บไม่ทำงาน
- cache URL รูปครุภัณฑ์ เพื่อลดการอ่านชีทรูปทุกครั้งที่ค้นหา
- เปลี่ยนการค้นหาครั้งแรกจากการสร้างดัชนีทั้งฐานข้อมูลเป็น `TextFinder` เฉพาะค่าที่สแกน แล้ว cache ตำแหน่งแถว 6 ชั่วโมง
- เปลี่ยนการหารูปจากอ่านทั้งชีทรูปเป็น `TextFinder` เฉพาะเลขครุภัณฑ์
- เผยแพร่ Apps Script เวอร์ชัน 79 ที่ deployment สาธารณะเดิม เพื่อคงลิงก์เดิมและสิทธิ์ `ทุกคน`

## สิ่งที่ยังต้องทดสอบก่อนเผยแพร่

- โหลดรอบบน Chrome/Android, Safari/iOS และเดสก์ท็อป
- ขอสิทธิ์กล้องและเลือกกล้องหลัง
- อ่าน QR จากกล้องและจากไฟล์ภาพ
- ตรวจรายการที่ยังไม่เคยตรวจและรายการที่ตรวจแล้ว
- บันทึกทั้ง 3 สถานะ และยืนยันข้อมูลในชีท
- ตรวจ deployment สำรอง พบว่าเป็น deployment เก่าสำหรับแอดมินและไม่ได้เปิดสาธารณะ จึงไม่นำมาใช้กับหน้าตรวจเพื่อป้องกันหน้าล็อกอิน/404 บนมือถือ
- ทดสอบแอดมิน: เปิด/ปิด/แก้ไข/รายงาน/รูป/แจ้งเตือน

## ข้อจำกัดที่ควรรู้

Google Apps Script เป็นบริการแบบ serverless จึงมี cold start, redirect และเวลาตอบไม่คงที่ ไม่สามารถรับประกันเร็วเท่ากัน 100% ทุกคำขอได้ การแก้ครั้งนี้ลดผลกระทบด้วย cache, ลดงานอ่านชีท และการยืนยันผล ไม่ได้ย้ายฐานข้อมูลหรือ API ออกจาก Google
